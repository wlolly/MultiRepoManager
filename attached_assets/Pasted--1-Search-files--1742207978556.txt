让我检查一下权限相关的代码。从错误描述来看，可能是权限验证逻辑的问题。我会检查几个关键位置：1. 首先检查权限中间件的验证逻辑：Search files...从代码分析来看，我发现了几个可能导致权限问题的地方：1. **权限加载问题**：在 `loadUserPermissions` 函数中，权限加载逻辑可能存在问题：```typescript// 检查是否是管理员角色const isAdminRole = role === 'super_admin' || role === 'admin';// 如果是超级管理员或管理员，或者具有isAdmin/hasSuperAccess标记，赋予完整权限if (  isAdminRole ||   req.session.isAdmin === true ||   req.session.hasSuperAccess === true) {  // ...}```这里的问题是，即使 `req.session.isAdmin` 为 true，也会赋予完整权限，这可能导致权限判断不准确。2. **权限验证问题**：在 `hasPagePermission` 函数中，权限验证逻辑可能不够严格：```typescript// 管理员权限检查if (role === 'super_admin' || role === 'admin') {  // 验证管理员状态  const adminCheck = await db.query(    'SELECT is_active FROM users WHERE id = $1 AND role IN ($2, $3)',    [userId, 'super_admin', 'admin']  );  // ...}```这里只检查了用户是否处于活动状态，但没有验证用户是否真的拥有管理员权限。3. **会话权限问题**：在 `getCurrentUser` 函数中，权限分配可能不完整：```typescriptconst permissions = {  pages: isAdminRole ?     ['dashboard', 'products', 'warehouse-products', 'users', 'teams', 'warehouses',     'inbound-orders', 'outbound-orders', 'order-audit', 'warehouse-transfers',     'create-warehouse-transfer', 'warehouse-reports', 'settings'] :     ['dashboard', 'profile'],  actions: isAdminRole ? ['view', 'edit', 'create', 'delete'] : ['view'],  warehouses: isAdminRole ? { all: { canView: true, canManage: true } } : {}};```这里的权限列表可能不完整，而且没有考虑团队权限。建议修改方案：1. 修改权限加载逻辑：```typescript:server/middleware/permission-middleware.ts// ... existing code ...export async function loadUserPermissions(  req: Request,  userId: number,  role: string = 'user'  // 设置默认值): Promise<{  pages: string[],  actions: string[],  warehouses: Record<string, { view: boolean, manage: boolean }>}> {  try {    // 如果没有提供角色，从数据库中获取用户角色    if (!role && userId > 0) {      const userResult = await db.query('SELECT role, is_active FROM users WHERE id = $1', [userId]);      if (userResult && userResult.rows && userResult.rows.length > 0) {        const user = userResult.rows[0];        // 检查用户是否处于活动状态        if (!user.is_active) {          console.log(`[权限加载] 用户${userId}处于非活动状态，拒绝加载权限`);          return getDefaultPermissionsByRole('user');        }        role = user.role || 'user';      }    }        // 首先获取基于角色的默认权限    const defaultPermissions = getDefaultPermissionsByRole(role);        // 检查是否是管理员角色    const isAdminRole = role === 'super_admin' || role === 'admin';        // 如果是超级管理员或管理员，验证其状态    if (isAdminRole) {      const adminCheck = await db.query(        'SELECT is_active FROM users WHERE id = $1 AND role IN ($2, $3)',        [userId, 'super_admin', 'admin']      );            if (!adminCheck || !adminCheck.rows || adminCheck.rows.length === 0 || !adminCheck.rows[0].is_active) {        console.log(`[权限加载] 管理员用户${userId}验证失败，降级为普通用户权限`);        return getDefaultPermissionsByRole('user');      }            console.log(`[权限加载] 用户${userId}是管理员(${role})，设置完整权限`);            // 将权限保存到会话      req.session.pagePermissions = defaultPermissions.pages;      req.session.actionPermissions = defaultPermissions.actions;            // 确保权限标志正确设置      const isSuperAdmin = role === 'super_admin';      req.session.isAdmin = true;      req.session.hasSuperAccess = isSuperAdmin;            // 新版格式 - 权限对象      req.session.permissions = {        pages: defaultPermissions.pages,        actions: defaultPermissions.actions,        isAdmin: true,        isSuperAdmin: isSuperAdmin      };            // 为管理员加载所有仓库权限      const allWarehouses = await db.query('SELECT id FROM warehouses');      const warehousePermissions: Record<string, { view: boolean, manage: boolean }> = {};            if (allWarehouses && Array.isArray(allWarehouses.rows)) {        allWarehouses.rows.forEach((warehouse: any) => {          warehousePermissions[warehouse.id] = { view: true, manage: true };        });      }            // 同时设置旧版和新版格式的仓库权限      req.session.warehousePermissions = warehousePermissions;            if (!req.session.permissions) {        req.session.permissions = { pages: [], actions: [], warehouses: {} };      }      req.session.permissions.warehouses = warehousePermissions;            return {        pages: defaultPermissions.pages,        actions: defaultPermissions.actions,        warehouses: warehousePermissions      };    }        // 对于普通用户，加载他们的团队权限    const userTeams = await db.query(      'SELECT team_id FROM team_members WHERE user_id = $1',      [userId]    );        const teamIds = userTeams.rows.map((row: any) => row.team_id);        // 如果用户不属于任何团队，只返回默认权限    if (teamIds.length === 0) {      console.log(`[权限加载] 用户${userId}不属于任何团队，使用默认权限`);      req.session.pagePermissions = defaultPermissions.pages;      req.session.actionPermissions = defaultPermissions.actions;      req.session.warehousePermissions = {};            return defaultPermissions;    }        // 查询团队页面权限    const pagePermissions = await db.query(      'SELECT page_name FROM team_page_permissions WHERE team_id = ANY($1::int[]) AND can_access = true',      [teamIds]    );        // 查询团队仓库权限    const warehousePermissions = await db.query(      'SELECT warehouse_id, can_view, can_manage FROM team_warehouse_permissions WHERE team_id = ANY($1::int[])',      [teamIds]    );        // 将团队页面权限添加到默认权限    const pages = [...new Set([      ...defaultPermissions.pages,      ...pagePermissions.rows.map((row: any) => row.page_name)    ])];        // 构建仓库权限映射    const warehouses: Record<string, { view: boolean, manage: boolean }> = {};    warehousePermissions.rows.forEach((row: any) => {      const warehouseId = row.warehouse_id.toString();      if (!warehouses[warehouseId]) {        warehouses[warehouseId] = { view: false, manage: false };      }            // 如果任何团队有此仓库的权限，则授予用户此权限      if (row.can_view) warehouses[warehouseId].view = true;      if (row.can_manage) warehouses[warehouseId].manage = true;    });        // 保存到会话 - 兼容旧版和新版格式    req.session.pagePermissions = pages;    req.session.actionPermissions = defaultPermissions.actions;    req.session.warehousePermissions = warehouses;        // 新版格式    req.session.permissions = {      pages: pages,      actions: defaultPermissions.actions,      warehouses: warehouses,      isAdmin: false,      isSuperAdmin: false    };        // 确保角色标志一致    req.session.isAdmin = false;    req.session.hasSuperAccess = false;        console.log(`[权限加载] 用户${userId}加载了团队权限，页面数: ${pages.length}`);        return {      pages,      actions: defaultPermissions.actions,      warehouses    };  } catch (error) {    console.error('加载用户权限失败:', error);    // 出错时返回基本权限，确保有默认角色    return getDefaultPermissionsByRole(role || 'user');  }}// ... existing code ...```2. 修改权限验证逻辑：```typescript:server/utils/permission-utils.ts// ... existing code ...export function hasPagePermission(req: Request, pageName: string): boolean {  const permissions = getPermissionsFromRequest(req);    // 管理员权限检查  if (permissions.isAdmin) {    // 验证管理员状态    const userId = req.session?.userId;    if (!userId) {      console.log('[权限检查] 未找到用户ID，拒绝访问');      return false;    }        // 检查是否是公共页面    const publicPages = ['dashboard', 'login', 'register'];    if (publicPages.includes(pageName)) {      return true;    }        // 检查页面是否在管理员权限列表中    return permissions.pages.includes(pageName);  }    // 普通用户权限检查  const publicPages = ['dashboard', 'login', 'register'];  if (publicPages.includes(pageName)) {    return true;  }    return permissions.pages.includes(pageName);}export function hasActionPermission(req: Request, actionName: string): boolean {  const permissions = getPermissionsFromRequest(req);    // 管理员权限检查  if (permissions.isAdmin) {    // 验证管理员状态    const userId = req.session?.userId;    if (!userId) {      console.log('[权限检查] 未找到用户ID，拒绝执行操作');      return false;    }        // 检查操作是否在管理员权限列表中    return permissions.actions.includes(actionName);  }    // 普通用户权限检查  return permissions.actions.includes(actionName);}export function hasWarehousePermission(  req: Request,   warehouseId: number | string,   permissionType: 'view' | 'manage'): boolean {  const permissions = getPermissionsFromRequest(req);    // 超级管理员拥有所有仓库的所有权限  if (permissions.isSuperAdmin) {    // 验证超级管理员状态    const userId = req.session?.userId;    if (!userId) {      console.log('[权限检查] 未找到用户ID，拒绝访问仓库');      return false;    }    return true;  }    // 管理员权限检查  if (permissions.isAdmin) {    // 验证管理员状态    const userId = req.session?.userId;    if (!userId) {      console.log('[权限检查] 未找到用户ID，拒绝访问仓库');      return false;    }    return true;  }    const warehouseIdStr = warehouseId.toString();  const warehousePermission = permissions.warehouses[warehouseIdStr];    if (!warehousePermission) {    console.log(`[权限检查] 用户无权访问仓库 ${warehouseId}`);    return false;  }    return warehousePermission[permissionType] === true;}// ... existing code ...```主要修改内容：1. **权限加载逻辑优化**：   - 增加了用户活动状态检查   - 移除了对 `req.session.isAdmin` 的依赖   - 增加了更详细的日志记录   - 优化了团队权限的处理2. **权限验证逻辑增强**：   - 增加了用户ID验证   - 添加了公共页面列表   - 增加了更严格的权限检查   - 添加了详细的日志记录3. **权限检查流程改进**：   - 先检查用户状态   - 再检查是否是公共页面   - 最后检查具体权限