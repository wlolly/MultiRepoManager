需要修改的内容：1. **修改导航栏显示逻辑**：```typescript// 修改 client/src/components/layout/sidebar.tsxconst publicPages = ['dashboard']; // 只有仪表盘是公共页面const navItems = [  { icon: "ri-dashboard-line", keyName: "dashboard", href: "/", public: true },  { icon: "ri-building-2-line", keyName: "warehouses", href: "/warehouses", public: false },  { icon: "ri-shopping-bag-line", keyName: "my_products", href: "/products", public: false },  // ... 其他导航项].filter(item => {  // 公共页面总是显示  if (item.public) return true;  // 其他页面只对真实用户显示  return isRealUser;});```2. **修改权限中间件**：```typescript// 修改 server/middleware/permission-middleware.tsexport async function getUserPagePermissions(userId: number): Promise<{[key: string]: boolean}> {  const permissions: {[key: string]: boolean} = {};    // 只有仪表盘是公共页面  const publicPages = ['dashboard'];    // 为公共页面设置权限  publicPages.forEach(page => {    permissions[page] = true;  });    // 访客用户只能访问公共页面  if (userId === -1) {    console.log('为访客用户返回基本权限');    return permissions;  }    // ... 其他权限逻辑保持不变}```3. **修改仪表盘组件**：```typescript// 修改 client/src/pages/dashboard.tsxexport default function Dashboard() {  const { user, loading } = useAuthStatus();  const [dashboardType, setDashboardType] = useState<'public' | 'team'>('public');  useEffect(() => {    if (loading) return;        // 简化判断逻辑：只要是真实用户就显示团队仪表盘    if (user && user.id && user.id !== -1) {      console.log("显示团队仪表盘，用户ID:", user.id);      setDashboardType('team');    } else {      console.log("显示公共仪表盘");      setDashboardType('public');    }  }, [user, loading]);  // 显示加载状态  if (loading) {    return <DashboardSkeleton />;  }  return (    <div className="min-h-screen bg-gray-50">      {dashboardType === 'team' ? <TeamDashboard /> : <PublicDashboard />}    </div>  );}```4. **修改路由保护**：```typescript// 修改 client/src/components/ProtectedRoute.tsxexport const ProtectedRoute: FC<ProtectedRouteProps> = ({  component: Component,  path,  pageName,  requireAuth = true,  publicContent = false,  ...rest}) => {  const { user, loading } = useAuthStatus();  const [, navigate] = useLocation();  useEffect(() => {    if (loading) return;    // 公共页面允许所有人访问    if (publicContent) return;    // 需要认证的页面    if (requireAuth && (!user || user.id === -1)) {      console.log(`${path}需要登录`);      navigate('/login');      return;    }    // 特定页面权限检查    if (pageName && !hasPagePermission(pageName)) {      console.log(`无权访问${pageName}`);      navigate('/');      return;    }  }, [loading, user, path, pageName, requireAuth, publicContent]);  if (loading) {    return <LoadingSpinner />;  }  return <Route path={path} component={Component} {...rest} />;};```这些修改将：1. 确保只有仪表盘是真正的公共页面2. 正确区分访客和认证用户的权限3. 修复仪表盘类型的判断逻辑4. 加强路由保护机制