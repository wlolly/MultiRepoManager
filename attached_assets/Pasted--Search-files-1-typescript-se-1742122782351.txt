让我检查一下管理员登录相关的代码。Search files...根据代码分析，我发现管理员登录出错可能有以下几个原因：1. **权限验证逻辑问题**：```typescript// server/auth.ts 中的验证中间件export function verifySession(req: Request, res: Response, next: NextFunction) {  // 极度简化的验证方法 - 所有请求都视为已认证的管理员用户  req.user = {     id: 1,     username: 'admin',    role: 'admin',    fullName: '系统管理员',    isActive: true  };```这里的验证逻辑过于简化，可能导致实际管理员用户无法正确登录。2. **角色定义不一致**：```typescript// shared/schema.tsexport const userRoleEnum = mysqlEnum("role", ["user", "admin", "super_admin"]);```但在前端代码中有时使用了不同的角色判断：```typescriptisAdmin = isRealUser && (user?.role === 'admin' || user?.role === 'super_admin');```3. **会话状态问题**：```typescript// server/routes.tsapiRouter.get("/auth/status", verifySession, (req, res) => {  const userId = req.session?.userId || -1;  const userRole = req.session?.userRole || 'anonymous';  const realAuthenticated = req.session?.realAuthenticated === true;```会话状态可能没有正确保存或更新。4. **权限检查中间件问题**：```typescript// server/middleware/permission-middleware.tsexport function isAdmin(req: Request, res: Response, next: NextFunction) {  // 简化版本：永远允许管理员访问  console.log('[简化验证] 管理员检查 - 永远通过');  next();}```权限检查过于简化，可能导致实际权限验证失效。建议的解决方案：1. **修复验证中间件**：```typescriptexport function verifySession(req: Request, res: Response, next: NextFunction) {  if (!req.session?.userId) {    return res.status(401).json({ message: '未登录' });  }    // 检查用户是否存在  const user = await db.query.users.findFirst({    where: eq(users.id, req.session.userId)  });    if (!user) {    return res.status(401).json({ message: '用户不存在' });  }    req.user = user;  next();}```2. **统一角色定义**：```typescript// shared/types.tsexport type UserRole = 'user' | 'admin' | 'super_admin';export interface User {  id: number;  username: string;  role: UserRole;  // ...其他字段}```3. **完善会话管理**：```typescriptexport async function loginUser(req: Request, res: Response) {  try {    const { username, password } = req.body;        // 验证用户    const user = await validateUser(username, password);    if (!user) {      return res.status(401).json({ message: '用户名或密码错误' });    }        // 设置会话    req.session.userId = user.id;    req.session.userRole = user.role;    req.session.authenticated = true;    req.session.realAuthenticated = true;        await req.session.save();        res.json({      id: user.id,      username: user.username,      role: user.role,      authenticated: true,      realAuthenticated: true    });  } catch (error) {    console.error('登录失败:', error);    res.status(500).json({ message: '登录失败' });  }}```4. **增强权限检查**：```typescriptexport function isAdmin(req: Request, res: Response, next: NextFunction) {  if (!req.user || (req.user.role !== 'admin' && req.user.role !== 'super_admin')) {    return res.status(403).json({ message: '需要管理员权限' });  }  next();}```