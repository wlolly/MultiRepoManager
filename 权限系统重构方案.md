# 权限系统重构方案

## 当前系统问题

1. **权限检查逻辑混乱**:
   - 服务器端定义了公共页面 `dashboard` 和 `products`，任何人都可以访问
   - 前端的ProtectedRoute组件没有正确处理真实用户与访客用户的区别
   - 服务器端假阳性登录和真实登录的判断标准与前端不一致

2. **数据库团队权限与前端认证缺乏协调**:
   - 团队权限在数据库中是按照团队组织的
   - 前端组件没有利用这些信息，而是自己判断
   - ProtectedRoute组件逻辑有问题，只依赖前端检查

## 基本概念定义

1. **用户类型**:
   - 真实用户：已登录且验证通过的用户（`authenticated === true`）
   - 管理员用户：角色为admin或super_admin的用户（`role === 'admin' || role === 'super_admin'`）
   - 访客用户：未登录或假阳性登录的用户（`id === -1 || fakePositive === true`）

2. **页面类型**:
   - 公共页面：任何人都可以访问（如仪表盘 `dashboard`）
   - 真实用户页面：只有真实登录用户才能访问（如产品管理 `products`）
   - 管理员页面：只有管理员才能访问（如用户管理 `users`）

3. **权限来源**:
   - 角色权限：基于用户角色的固定权限
   - 团队权限：基于用户所属团队的动态权限
   - 资源权限：基于特定资源（如仓库）的访问权限

## 解决方案设计

### 后端修改

#### 1. 更新 `auth.ts`

- 添加明确的用户身份验证字段 `realAuthenticated`
- 区分假阳性登录和真实登录
- 在会话中存储真实登录标记

```typescript
// 会话中增加realAuthenticated字段
req.session.realAuthenticated = true; // 只有真正认证的用户才设置为true
```

#### 2. 更新 `permission-middleware.ts`

- 修改公共页面定义，仅保留`dashboard`为公共页面
- 将 `products` 设置为需要真实用户的页面
- 添加管理员用户的快速通道

```typescript
// 定义公共页面
const publicPages = ['dashboard']; // 只有仪表盘是真正的公共页面

// 定义需要真实用户登录的页面
const authenticatedPages = [
  'products', 'warehouses', 'warehouse-products', 
  'inbound-orders', 'outbound-orders', 'warehouse-transfers'
];

// 首先检查是否管理员或真实用户
const isAdmin = req.session?.userRole === 'admin';
const isRealAuthenticated = req.session?.realAuthenticated === true || isAdmin;
```

#### 3. 创建新的权限API端点

**GET /api/auth/status**:
返回用户的真实认证状态和角色信息

```typescript
app.get('/api/auth/status', (req, res) => {
  const userId = req.session?.userId || -1;
  const userRole = req.session?.userRole || 'anonymous';
  const realAuthenticated = req.session?.realAuthenticated === true;
  
  res.json({
    userId,
    userRole,
    realAuthenticated,
    isGuest: userId === -1,
    isAdmin: userRole === 'admin' || userRole === 'super_admin'
  });
});
```

### 前端修改

#### 1. 更新 `use-permissions.ts` 钩子

- 添加 `isRealAuthenticated` 状态
- 从新的API端点获取状态
- 提供辅助函数判断用户类型

```typescript
// 添加到PermissionsHook接口
isRealAuthenticated: boolean;
isAdmin: boolean;
isGuestUser: boolean;

// 实现逻辑
const fetchAuthStatus = async () => {
  const response = await fetch('/api/auth/status');
  const data = await response.json();
  
  setIsRealAuthenticated(data.realAuthenticated);
  setIsAdmin(data.isAdmin);
  setIsGuestUser(data.isGuest);
};
```

#### 2. 更新 `ProtectedRoute.tsx` 组件

- 基于用户类型强制正确的路由行为
- 使用新的权限状态判断

```typescript
useEffect(() => {
  if (loading) return;

  let permissionResult = true;
  
  // 登录检查：需要真实认证的页面
  if (requireAuth && !publicContent) {
    // 如果不是真实认证用户，重定向到仪表盘
    if (!isRealAuthenticated) {
      console.log(`${path}页面需要真实登录，重定向到仪表盘`);
      navigate('/');
      return false;
    }
  }
  
  // 权限检查：需要特定页面权限
  if (pageName && !hasPagePermission(pageName)) {
    console.log(`用户无权访问${pageName}页面，重定向到首页`);
    navigate('/'); 
    return false;
  }
  
  return permissionResult;
}, [
  isRealAuthenticated,
  requireAuth,
  publicContent,
  pageName,
  hasPagePermission,
  navigate
]);
```

#### 3. 更新 `App.tsx` 页面路由

- 对所有需要真实用户的页面使用 `requireAuth={true}` 和 `publicContent={false}`
- 用管理员检查包装管理员页面

```typescript
// 仪表盘 - 唯一真正的公共页面
<ProtectedRoute exact path="/" component={Dashboard} pageName="dashboard" requireAuth={false} publicContent={true} />

// 产品页面 - 只有真实用户才能访问
<ProtectedRoute path="/products" component={ProductsPage} pageName="products" requireAuth={true} publicContent={false} />

// 管理员页面 - 需要管理员权限
<ProtectedRoute path="/users" component={Users} pageName="users_teams" requireAuth={true} publicContent={false} adminOnly={true} />
```

#### 4. 更新 Sidebar 组件

- 只向真实用户显示产品管理等页面链接
- 根据权限动态生成导航菜单

```typescript
// 修改导航菜单过滤逻辑
{navItems
  .filter(item => 
    item.href === "/" ||  // 仪表盘总是显示
    (isRealAuthenticated && // 真实用户可以看到产品等页面
     (!item.adminOnly || isAdmin)) // 管理员可以看到标记为管理员的页面
  )
  .map((item) => (...))}
```

## 数据库集成

### 1. 统一团队权限表结构

确保以下表在正确创建并使用：

- `team_page_permissions`: 团队的页面访问权限
- `team_warehouse_permissions`: 团队的仓库访问权限
- `team_members`: 团队成员关系

### 2. 创建权限初始化脚本

创建一个脚本确保数据库中存在必要的权限记录，特别是对默认团队：

```typescript
// 确保管理员团队有所有页面权限
async function ensureAdminTeamPermissions() {
  const adminTeam = await db.query(`SELECT id FROM teams WHERE name = 'Admin Team'`);
  if (!adminTeam.rows.length) {
    // 创建管理员团队
    // ...
  }
  
  // 为管理员团队设置所有页面权限
  const pages = ['dashboard', 'products', 'warehouses', /* ... */];
  for (const page of pages) {
    await db.query(`
      INSERT INTO team_page_permissions (team_id, page_name, can_access)
      VALUES (?, ?, true)
      ON DUPLICATE KEY UPDATE can_access = true
    `, [adminTeam.rows[0].id, page]);
  }
}
```

## 实施步骤

1. 更新 `auth.ts` 添加真实认证标记
2. 创建 `/api/auth/status` 端点
3. 更新 `permission-middleware.ts` 修改权限逻辑
4. 更新 `use-permissions.ts` 钩子添加新的状态
5. 重构 `ProtectedRoute.tsx` 组件
6. 更新 `App.tsx` 中的路由定义
7. 更新 Sidebar 组件的导航过滤逻辑
8. 创建并运行权限初始化脚本

## 后续优化

1. 添加更详细的日志记录，便于调试
2. 在前端UI中明确指示用户当前的身份状态
3. 为管理员提供直观的权限管理界面
4. 实现更细粒度的操作权限控制